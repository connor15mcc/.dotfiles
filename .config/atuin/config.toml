style = "compact"
keymap_mode = "vim-insert"
filter_mode = "session"
history_filter = ["^kubectl create secret"]
inline_height = 25
update_check = false

search_mode = "daemon-fuzzy"

[daemon]
enabled = true
autostart = true

[ai]
enabled = false
