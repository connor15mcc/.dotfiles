layout {
  default_tab_template {
    children 
    pane size=1 borderless=true {
      plugin location="file:$HOME/.config/zellij/plugins/zjstatus.wasm" {
        format_left   "{mode} #[fg=#31748f,bold]{session}"
        format_center "{tabs}"
        format_right  "{datetime}"
        format_space  ""

        border_enabled  "false"

        mode_normal  "#[bg=#31748f] "
        mode_tmux    "#[bg=#f6c177] "

        // formatting for inactive tabs
        tab_normal              "#[fg=#31748f] #{index}"
        tab_normal_fullscreen   "#[fg=#31748f] #{index} {fullscreen_indicator}"

        // formatting for the current active tab
        tab_active              "#[fg=#f6c177,bold,italic] #{index}"
        tab_active_fullscreen   "#[fg=#f6c177,bold,italic] #{index} {fullscreen_indicator}"

        tab_fullscreen_indicator "[]"

        datetime        "#[fg=#31748f,bold] {format} "
        datetime_format "%A %D @ %I:%M%P"
        datetime_timezone "America/New_York"
      }
    }
  }
}
